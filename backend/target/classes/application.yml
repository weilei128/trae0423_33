server:
  port: 10023

spring:
  application:
    name: jewelry-system

  datasource:
    url: jdbc:mysql://49.235.161.106:10011/db3?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: wl
    password: Wl1733811
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: 49.235.161.106
      port: 10012
      password:
      timeout: 10000ms
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

  sql:
    init:
      mode: never

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.jewelry.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
      id-type: auto

jwt:
  secret: jewelry-secret-key-for-jwt-token-2024
  expiration: 86400000
